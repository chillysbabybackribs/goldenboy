# Agent Examples

Status: reference / experimental only.

Files in this directory are example workflow artifacts and design aids. They are not part of the live Goldenboy `AgentRuntime` path unless a future host-side feature explicitly wires them in.

- `yahoo-local-badge.workflow.json` is a sample deterministic workflow artifact kept for documentation and experimentation.
- Do not treat these files as active runtime contracts.
