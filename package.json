{
  "name": "goldenboy",
  "version": "1.0.0",
  "description": "Goldenboy — local Codex-backed AI workbench with chat, browser, and terminal surfaces",
  "main": "dist/main/main/main.js",
  "scripts": {
    "build:main": "tsc -p tsconfig.main.json",
    "build:preload": "tsc -p tsconfig.preload.json",
    "build:renderer": "tsc -p tsconfig.renderer.json",
    "prebuild": "npm run clean",
    "build": "npm run build:main && npm run build:preload && npm run build:renderer && npm run copy:html",
    "copy:html": "node scripts/copy-renderer.js",
    "start": "npm run build && electron .",
    "dev": "node scripts/dev.js",
    "test": "vitest run",
    "test:e2e:determinism": "node scripts/e2e/determinism/run.js",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/xterm": "^6.0.0",
    "electron": "^41.2.0",
    "jsdom": "^26.1.0",
    "typescript": "^5.8.0",
    "vitest": "^4.1.4"
  },
  "dependencies": {
    "node-pty": "^1.1.0"
  }
}
